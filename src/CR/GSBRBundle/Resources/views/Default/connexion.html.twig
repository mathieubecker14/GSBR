<link rel="stylesheet" href="{{asset('bundles/GSBR/css/bootstrap.css')}}">


<div class="row connexion">
    {% if error %}
        <div class="alert alert-danger">{{ error.message }}</div>
    {% endif %}
    <div class="col-sm-6 col-sm-offset-3">
        <div class="row">
            <div class="text-center">
                <img src="{{asset('bundles/GSBR/img/logo.png')}}" alt="Logo">
            </div>
        </div>
        <form action="{{ path('crgsbr_connexion_check') }}" method="post">
            <div class="form-group">
                <label for="username">LOGIN</label>
                <input type="text" class="form-control" id="username" name="_username">
            </div>
            <div class="form-group">
                <label for="password">MOT DE PASSE</label>
                <input type="password" class="form-control" id="password" name="_password">
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-lg btn-primary">Connexion</button>
            </div>
        </form>
    </div>
</div>

